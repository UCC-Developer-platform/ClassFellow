# ClassFellow - Project Assignment & Master Task Register (.agent.md)

**Project Name**: ClassFellow  
**Target Market**: Small & Medium Private Schools, Colleges, and Tuition Academies in Punjab, Pakistan  
**Technology Foundation**: Python 3.11+ | CustomTkinter (Desktop MVP) -> Django (Web Platform) | SQLite (WAL Mode) -> PostgreSQL | ReportLab (A4 3-Panel Vouchers)  
**Status**: Phase 9 Master Roadmap Approved (`docs/roadmaps/PHASE_09_VALIDATION_REFINEMENT_AND_MAINTENANCE_ROADMAP.md`) | Track 01 Physical USB Audit IN PROGRESS | Active Defect Register: [`docs/troubleshooting/BUGS_AND_RESOLUTION_REGISTER.md`](file:///c:/04_Classfellow/docs/troubleshooting/BUGS_AND_RESOLUTION_REGISTER.md)

---

## 1. Master Task Register

### Category 1: Formal Modular SRS Specifications (`docs/srs/`)
- [x] **Task SRS-00: Modular Architecture & System Overview Specification**
  - **File**: `docs/srs/SRS_00_Overview_and_Modular_Architecture.md`
  - **Scope**: Product vision, commercial tiering (Fee-Only Edition, Fee+Attendance Edition, Full Professional Suite), plug-and-play module plugin design, and dynamic feature-flag licensing (`config/modules.json`).
- [x] **Task SRS-01: Core Kernel, Database Engine & Infrastructure Specification**
  - **File**: `docs/srs/SRS_01_Core_Kernel_and_Database_Spec.md`
  - **Scope**: SQLite connection harness (`WAL` mode, `foreign_keys=ON`, `synchronous=NORMAL`), `Decimal` text adapters/converters, schema migration versioning (`PRAGMA user_version` / `schema_migrations`), Git branching workflow, CI/CD pipeline, and 3-tier backup architecture.
- [x] **Task SRS-02: Student & Enrollment Module Specification**
  - **File**: `docs/srs/SRS_02_Student_and_Enrollment_Spec.md`
  - **Scope**: Dual operational models: Private School model (Class/Section based, flat monthly fee, daily class attendance) vs. Tuition Academy model (Subject/Batch based, per-course fee, session attendance). Student registration, profile search, and guardian data.
- [x] **Task SRS-03: Fee & Receipt Module Specification**
  - **File**: `docs/srs/SRS_03_Fee_and_Receipt_Module_Spec.md`
  - **Scope**: Dynamic fee categories (`fee_heads`), master invoices (`fee_invoices`), itemized lines (`fee_invoice_items`), auditable payment ledger (`payments` enforcing `ON DELETE RESTRICT`), two-tier due dates (`due_date` & `valid_until`), currency in words via `num2words`, ReportLab 3-panel A4 voucher geometry, and Urdu script reshaping (`arabic-reshaper`/`python-bidi`).
- [x] **Task SRS-04: Attendance & Notification Module Specification**
  - **File**: `docs/srs/SRS_04_Attendance_and_Notification_Spec.md`
  - **Scope**: Class and batch attendance grids, database uniqueness constraint `Unique(enrollment_id, date)`, monthly attendance calculations, absence alerts, and WhatsApp-ready notification message templates.
- [x] **Task SRS-05: Examination & Report Card Module Specification**
  - **File**: `docs/srs/SRS_05_Examination_and_ReportCard_Spec.md`
  - **Scope**: Exam sessions, class/batch-specific subject maximum marks, marks entry grid, configurable grade boundary rules, pass/fail policies, and bilingual A4 report card PDF rendering.

*(Category 1: Formal Modular SRS Specifications is 100% COMPLETE)*

---

### Category 2: GitHub Repository & Version Control Setup
- [x] **Task GIT-01: Local Git Repository Initialization**
  - Initialized local git repository inside project workspace (`git init -b main`).
- [x] **Task GIT-02: Production-Grade `.gitignore`**
  - Configured comprehensive ignore rules: `.venv/`, `*.pyc`, `__pycache__/`, `data/*.db`, `data/backups/`, `logs/`, `.env`, and IDE config files.
- [x] **Task GIT-03: Git Branching Strategy & Initial Commit**
  - Standardized repository branches (`main`, `develop`), created `README.md`, committed baseline, and pushed to remote `https://github.com/UCC-Developer-platform/ClassFellow.git`.
- [x] **Task GIT-04: GitHub Actions CI/CD Pipeline**
  - Created `.github/workflows/ci.yml` executing automated `pytest` and `flake8` test suites on Windows and Ubuntu matrices.

*(Category 2: GitHub Repository & Version Control Setup is 100% COMPLETE)*

---

### Category 3: 3-Tier Backup Architecture Setup
- [x] **Task BKP-01: Tier 1 - Local Daily Automated Backup**
  - Implement automated snapshot routine in `services/backup_service.py` triggered on application launch and shutdown.
  - **Critical Rule**: Must use SQLite's native online backup API (`src_conn.backup(dest_conn)`) to ensure uncommitted WAL transactions are snapshotted cleanly without file-locking.
  - Destination: `data/backups/daily/classfellow_backup_YYYYMMDD_HHMMSS.db`.
  - Retention: Automated rolling 30-day snapshot retention with older backup pruning.
- [x] **Task BKP-02: Tier 2 - Manual USB Snapshot Export / Import**
  - Provide a one-click admin export routine producing structured ZIP archives containing database snapshot, `manifest.json`, and SHA-256 cryptographic checksum with atomic restore sequence purging lingering `-wal` and `-shm` companion files.
- [x] **Task BKP-03: Tier 3 - Automated Cloud Backup (Google Drive Sync)**
  - Background asynchronous worker service (`GoogleDriveSyncWorker` / `CloudSyncProvider`) using socket connectivity probe (`check_network_connectivity`) to upload database snapshots whenever active internet connectivity is detected without blocking desktop UI.

*(Category 3: 3-Tier Backup Architecture Setup is 100% COMPLETE)*

---

### Category 4: Module-Based Commercial Architecture
- [x] **Task MOD-01: Module Registry & Feature Flag Manager (`config/modules.json`)**
  - Build module license configuration schema supporting commercial product packaging:
    - **Tier 1: Fee-Only Edition** (`students`: enabled, `fees`: enabled, `attendance`: disabled, `exams`: disabled).
    - **Tier 2: Standard Edition** (`students`: enabled, `fees`: enabled, `attendance`: enabled, `exams`: disabled).
    - **Tier 3: Professional Suite** (`students`: enabled, `fees`: enabled, `attendance`: enabled, `exams`: enabled).
- [x] **Task MOD-02: Dynamic UI Sidebar Navigation & Single-Window Swapping**
  - The CustomTkinter navigation sidebar inspects `modules.json` during initialization. Unlicensed modules are cleanly hidden from navigation menus without causing application exceptions. Dynamic workspace swapping engine in `app/app.py` switches views within `self.workspace_frame`.
- [x] **Task MOD-03: Decoupled Domain Service Boundaries & Read-Only Modular UI Layer**
  - Strictly isolate `fee_service.py`, `attendance_service.py`, and `exam_service.py`. Modules communicate only through DTOs and database contracts, preventing circular dependencies. Modular CustomTkinter views in `ui/` (`base_view.py`, `dashboard_view.py`, `student_view.py`, `fee_view.py`, `attendance_view.py`, `exam_view.py`, `settings_view.py`) implement read-only tabular grids and focus-trapped mutation modals (`CTkToplevel`) with `transient()` and `grab_set()`.

*(Category 4: Module-Based Commercial Architecture is 100% COMPLETE)*


---

### Category 5: Phase 1 Codebase Scaffolding & Setup
- [x] **Task CODE-01: Modular Directory Hierarchy Creation**
  - Established the official project directory structure (`app/`, `assets/fonts/`, `services/`, `models/`, `ui/`, `reports/`, `tests/`, `config/`, `data/backups/`, `docs/srs/`).
- [x] **Task CODE-02: Environment & Pinned Dependencies (`requirements.txt`)**
  - Created `.venv` virtual environment and installed pinned packages: `customtkinter`, `reportlab`, `arabic-reshaper`, `python-bidi`, `num2words`, `openpyxl`, `pytest`, `pytest-cov`.
- [x] **Task CODE-03: Windows High-DPI Scaling Hook (`app/app.py`)**
  - Implemented guarded `init_windows_dpi()` call in `app/app.py` with CustomTkinter shell, theme configuration loading, and dynamic module navigation.
- [x] **Task CODE-04: Thread-Safe SQLite Connection Manager (`database.py`)**
  - Implemented `get_connection()` with `Decimal` adapters/converters, WAL mode, foreign keys, synchronous normal, and native `create_backup_snapshot()` online backup API.
- [x] **Task CODE-05: Urdu Ligature Reshaping Pipeline (`app/reports/urdu_formatter.py`)**
  - Implemented `format_urdu()` combining `arabic-reshaper` and `python-bidi` for ReportLab A4 PDF rendering.
- [x] **Task CODE-06: Automated Testing Infrastructure (`tests/`)**
  - Configured `pytest.ini`, `conftest.py`, and authored 8 unit tests across `test_database.py` and `test_urdu_formatter.py` (100% passing).

*(Category 5: Phase 1 Codebase Scaffolding & Setup is 100% COMPLETE)*

---

### Category 6: Core Domain Services Implementation
- [x] **Task DOM-01: Initial Schema Migration (`services/schema_service.py`)**
  - Implemented `migration_v1_initial_schema` creating relational tables and indexes for SRS_02 (`students`, `enrollments`, `class_groups`, `academic_sessions`) and SRS_03 (`fee_heads`, `fee_invoices`, `fee_invoice_items`, `payments`).
  - Managed via `PRAGMA user_version` tracking and `migrate_to_latest(conn)`.
- [x] **Task DOM-02: Student Domain Service (`services/student_service.py`)**
  - Implemented `StudentService` with atomic student registration, initial enrollment creation, auto-incrementing admission numbers (`CF-YYYY-XXXX`), Pakistani phone normalization/validation (`^03[0-9]{9}$`), and high-performance multi-field search (`admission_number`, names, Urdu script, phone, roll number).
- [x] **Task DOM-03: Student Domain Automated Test Suite (`tests/test_student_service.py`)**
  - Authored 15 dedicated unit and integration tests verifying schema migrations, Pakistani phone normalization, admission number sequences, uniqueness enforcement, atomic enrollments, search filtering, and student status updates (100% passing).
- [x] **Task DOM-04: Fee & Invoicing Domain Service (`services/fee_service.py`)**
  - Implemented `FeeService` providing itemized monthly invoicing, discount deduction, two-tier due dates, cash payment collection, sequential receipt generation (`REC-YYYY-XXXXX`), payment reversals with audit logging, defaulter reporting, and `ON DELETE RESTRICT` ledger protection.
- [x] **Task DOM-05: Currency in Words Utility (`reports/currency_words.py`)**
  - Implemented `amount_in_words_en()` using `num2words` for bilingual voucher footers and Pakistani financial formatting.
- [x] **Task DOM-06: ReportLab 3-Panel A4 Fee Voucher Generator (`app/reports/fee_voucher_generator.py`)**
  - Implemented `FeeVoucherGenerator` rendering School Copy, Bank Copy, and Student Copy on a single portrait A4 sheet ($595.28 \times 841.89$ pt), complete with dashed cut lines, institution branding, itemized tables, amount in words, and Urdu name shaping via `format_urdu()`.
- [x] **Task DOM-07: Fee Subsystem Automated Test Suite (`tests/test_fee_service.py`)**
  - Authored 8 dedicated unit and integration tests covering currency words, monthly invoicing, status state machine transitions (`Unpaid` -> `Partially Paid` -> `Paid` -> `Overpaid`), payment reversals, `ON DELETE RESTRICT` constraints, and 3-panel A4 PDF generation.
- [x] **Task DOM-08: Migration v2 - Attendance & Batch Schema (`services/schema_service.py`)**
  - Implemented `migration_v2_attendance_schema` establishing `batch_sessions` and `attendance_records` tables with strict `UNIQUE(enrollment_id, attendance_date)` constraint and performance indexes.
- [x] **Task DOM-09: Attendance Domain Service (`services/attendance_service.py`)**
  - Implemented `AttendanceService` featuring high-speed bulk roster loading with default `'Present'` status, atomic `UPSERT` on conflict, monthly attendance percentage calculations, and zero-cost bilingual WhatsApp absence notification payloads with `wa.me` deep-links.
- [x] **Task DOM-10: Attendance Automated Test Suite (`tests/test_attendance_service.py`)**
  - Authored 8 dedicated unit and integration tests verifying migration v2, default 'Present' roster loading, atomic `UPSERT` conflict handling without duplicate records, database constraint enforcement, monthly percentages, and WhatsApp URL generation.

- [x] **Task DOM-11: Migration v3 - Examination & Report Card Schema (`services/schema_service.py`)**
  - Implemented `migration_v3_exam_schema` establishing `subjects`, `exams`, `exam_subjects`, `grading_tiers`, and `marks` tables with performance indexes and baseline subjects.
- [x] **Task DOM-12: Examination Domain Service (`services/exam_service.py`)**
  - Implemented `ExamService` supporting master subjects, exam cycles, dynamic exam-subject mark limits with validation ($0 \le \text{marks\_obtained} \le \text{maximum\_marks}$), configurable grading tiers, atomic marks ledger recording, aggregate term percentages, and class rank computation.
- [x] **Task DOM-13: ReportLab Bilingual A4 Terminal Report Card Generator (`app/reports/report_card_generator.py`)**
  - Implemented `ReportCardGenerator` and `generate_report_card_pdf` producing single-sheet A4 portrait terminal report cards ($595.28 \times 841.89$ pt) with institutional branding, student profile box, subject results table, scorecard metric blocks, attendance percentages, pre-shaped Urdu teacher remarks, and 3 signature lines.
- [x] **Task DOM-14: Examination Subsystem Automated Test Suite (`tests/test_exam_service.py`)**
  - Authored 7 comprehensive unit and integration tests covering migration v3 schema and versioning, subject management, exam subject limit constraints, grading tier boundary evaluation, strict marks upper-bound validation, class rank ordering with joint ties, and ReportLab A4 PDF byte generation (100% passing).

*(Category 6: Core Domain Services Implementation - Student, Fee, Attendance & Examination Subsystems are 100% COMPLETE)*

---

### Category 7: Standalone Packaging & Pilot Demo Seeding
- [x] **Task PKG-01: Windows 64-bit PyInstaller Specification (`classfellow.spec`)**
  - Configured clean PyInstaller build spec targeting Windows 64-bit windowed executable (`console=False`).
  - Bundled critical asset trees: `assets/fonts/` (Urdu and Unicode TrueType fonts) and `config/` (`theme.json` and commercial `modules.json`).
  - Collected CustomTkinter data assets via `collect_data_files("customtkinter")`.
  - Added hidden imports for `customtkinter`, `reportlab`, `arabic_reshaper`, `bidi`, `num2words`, `sqlite3`, domain services, UI views, and reports.
- [x] **Task PKG-02: Punjab Pilot Database Demo Seeder (`scripts/seed_demo_data.py`)**
  - Created self-contained CLI seeding script populating a realistic Punjab pilot dataset:
    - 1 Academic Session (2026-2027)
    - 3 Class Groups (Class 9 - Green, Class 10 - Gold, Tuition Batch - 9th Physics)
    - 15 Enrolled Students with bilingual English/Urdu names and normalized mobile numbers (`0300...`)
    - Monthly Fee Invoices for 2026-04 with payment ledger records (5 Paid, 5 Partial, 5 Unpaid Defaulters)
    - 5-Day Attendance Records (75 total records: Present, Absent, Late, Leave)
    - Graded Examination with 35 subject mark records and BISE Punjab class rankings
  - Handled Windows terminal encoding (`cp1252`) cleanly without emoji decoding crashes.
  - Implemented idempotent `get_or_create` helpers to preserve schema pre-seeded data.
- [x] **Task PKG-03: Demo Seeder Automated Test Suite (`tests/test_seed_demo_data.py`)**
  - Authored automated integration test verifying database completeness, session activation, class types, student phone normalization, invoice status distribution (5 Paid, 5 Partial, 5 Unpaid), defaulters list, attendance record counts, and exam ranking sorting.
- [x] **Task PKG-04: Documentation & CLI Instructions (`README.md`)**
  - Updated README with demo pilot seeding instructions and PyInstaller standalone build commands.

*(Category 7: Standalone Packaging & Pilot Demo Seeding is 100% COMPLETE)*

---

### Category 8: Pilot Deployment, Performance Telemetry & Bulk Onboarding
- [x] **Task PLT-01: Windows Inno Setup Compiler Packaging (`installer.iss`)**
  - Configured `classfellow.spec` with `onedir` `COLLECT` block producing `dist/ClassFellow/` containing `ClassFellow.exe` and `_internal/`.
  - Executed `pyinstaller classfellow.spec --clean` producing a verified standalone distribution.
  - Authored `installer.iss` configured for Inno Setup 6:
    - Target: `ClassFellow` (v1.0.0-mvp).
    - Default installation directory: `{autopf}\ClassFellow` with per-user dialogue overrides.
    - Packages `ClassFellow.exe`, `_internal/`, `assets/`, `config/`.
    - Generates Desktop and Start Menu shortcuts.
    - Clean uninstall directives while safeguarding institutional database snapshots (`data/`).
  - Documented Inno Setup CLI compilation steps in `README.md`.
- [x] **Task PLT-02: Bulk Student & Enrollment Excel Importer**
  - Implemented `StudentImporterService` in `services/importer_service.py`:
    - `generate_excel_template()` creates styled `.xlsx` template with "Student Roster" and "Instructions" sheets, column formatting, and sample rows.
    - `import_students_from_excel()` ingests `.xlsx` and `.csv` files with flexible header normalization regex, Pakistani mobile normalization (`03XXXXXXXXX`), automatic sequential admission numbers (`CF-YYYY-XXXX`), date parsing, and decimal discount validation.
    - Resilient batch processing returns structured summary (`total_rows`, `imported_count`, `failed_count`, `errors`).
  - Integrated into `ui/student_view.py` with "📥 Import Excel" and "📄 Template" action buttons, `StudentImportModal` (file browser, class/session pickers), and `ImportSummaryModal` (KPI metrics tiles, scrollable error logs).
  - Hardened modal lifecycle in `ui/base_view.py` with deferred window destruction preventing `bad window path name` Tcl errors.
  - Authored 7 unit/integration tests in `tests/test_importer_service.py` and UI modal tests in `tests/test_ui_views.py` (84 total passing tests).
- [x] **Task PLT-03: Performance Profiling & Query Benchmarking**
  - Created standalone benchmark script `scripts/benchmark_db.py`:
    - Generates synthetic load of 5,000 students, 20 class groups, 50,000 fee invoices, 35,000 payments, and 100,000 attendance records via chunked `executemany` batches in ~1.85s.
    - Benchmarks core queries against strict SLA (< 100ms): Defaulters Ledger Scan (30.70ms), Student Multi-Field Search (23.36ms), Class Attendance Roster (2.69ms), and Batch Fee Invoice Creation (11.89ms).
    - Verifies `PRAGMA integrity_check` (clean 'ok') and `PRAGMA wal_checkpoint(PASSIVE)` under concurrent WAL conditions.
    - Added covering indexes `idx_fee_invoices_valid_until`, `idx_fee_invoices_session_valid`, and `idx_payments_invoice_status_amount`.
    - Renders clean, cp1252-safe ASCII telemetry report.
  - Integrated fast, scoped CI test suite in `tests/test_performance_benchmark.py` running in < 0.6s to protect against query regressions (87 total passing tests).

---

### Category 9: Phase 5 Multi-User Web Architecture Preparation
- [x] **Task WEB-01: Headless Domain Service Decoupling Audit**
  - Audited `services/student_service.py`, `services/fee_service.py`, `services/attendance_service.py`, `services/exam_service.py`, `services/importer_service.py`, `services/backup_service.py`, and `services/schema_service.py`.
  - Verified static AST zero-dependency isolation on `tkinter`, `customtkinter`, or UI dialogs.
  - Authored `tests/test_headless_services.py` with static AST parsing and dynamic headless runtime mocking blocking Tkinter across all core domain flows.
- [x] **Task WEB-02: PostgreSQL 16 Relational DDL & Migration Specification**
  - Created `database/postgres/schema_v1_postgres.sql` translating SQLite schema into production-ready PostgreSQL 16 DDL:
    - Primary keys: `BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY`.
    - Monetary fields: `NUMERIC(12, 2) NOT NULL` across all fee, discount, surcharge, and marks columns.
    - Boolean fields: Native `BOOLEAN NOT NULL DEFAULT TRUE/FALSE`.
    - Timestamps: `TIMESTAMPTZ NOT NULL DEFAULT CURRENT_TIMESTAMP` with automated `set_updated_at_timestamp()` triggers.
    - Referential integrity: `ON DELETE RESTRICT` on `payments` ledger and enrollments, composite `UNIQUE` constraints on attendance, invoices, exam subjects, and marks.
    - Performance indexes: Ported 20 explicit B-Tree covering indexes (`CREATE INDEX ... USING btree`).
    - Seed data: Idempotent initialization of baseline `fee_heads` and bilingual `subjects` catalog.
  - Authored automated DDL syntax verification test suite in `tests/test_postgres_ddl.py` (12 passing tests).

*(Category 9: Phase 5 Multi-User Web Architecture Preparation is 100% COMPLETE)*

---

### Category 10: Phase 5 Django Web Scaffolding & ORM Mapping
- [x] **Task DJ-01: Django 5 Project Scaffolding & PostgreSQL Configuration**
  - Added pinned dependencies `django>=5.0,<5.2` and `psycopg[binary]>=3.1.18` to `requirements.txt` and installed into `.venv`.
  - Initialized `classfellow_web/` project structure: `manage.py`, `config/` (`settings.py`, `urls.py`, `wsgi.py`, `asgi.py`), and 6 modular domain apps under `apps/` (`accounts`, `core`, `students`, `fees`, `attendance`, `examinations`).
  - Configured custom user model `AUTH_USER_MODEL = 'accounts.User'` with institutional roles (`Admin`, `Principal`, `Cashier`).
  - Configured PostgreSQL 16 connection settings using environment variables (`DB_NAME`, `DB_USER`, `DB_PASSWORD`, `DB_HOST`, `DB_PORT`) with test-runner SQLite fallback.
- [x] **Task DJ-02: Django ORM Relational Models & Integrity Constraints Mapping**
  - Implemented all 15 relational domain models across the 6 apps mirroring `database/postgres/schema_v1_postgres.sql`:
    - Set exact `db_table` names matching the PostgreSQL DDL.
    - Standardized primary keys to `BigAutoField`.
    - Enforced `DecimalField(max_digits=12, decimal_places=2)` across all financial/monetary fields and `DecimalField(max_digits=6, decimal_places=2)` on marks.
    - Configured `on_delete=models.RESTRICT` across `Payment.invoice`, `Enrollment.student`, `Enrollment.class_group`, and `FeeInvoice.enrollment`.
    - Defined composite `UniqueConstraint` entries: `(enrollment, attendance_date)`, `(enrollment, month_year)`, `(exam, class_group, subject)`, `(student, class_group, session)`, and `(exam_subject, enrollment)`.
    - Configured all 20 explicit B-Tree covering indexes across models.
  - Executed `python classfellow_web/manage.py makemigrations` creating clean, unconflicted initial migration graphs across all 6 apps.
  - Authored automated test suite `tests/test_django_models.py` (9 tests passing).
- [x] **Task DJ-03: Headless Service Layer Migration (`apps/*/services.py`)**
  - Implemented `StudentWebService` in `classfellow_web/apps/students/services.py`:
    - `register_student()`: Validates Pakistani mobile numbers (`^03\d{9}$`), auto-generates sequential admission numbers (`CF-YYYY-XXXX`), and atomically enrolls students inside `transaction.atomic()`.
    - `search_students()`: Multi-field `Q` query filtering by admission number, bilingual English/Urdu names, guardian phone, and roll number.
    - `update_student_status()`: Manages status transitions (`Active`, `Withdrawn`, `Transferred`) and toggles student active state.
  - Implemented `FeeWebService` in `classfellow_web/apps/fees/services.py`:
    - `generate_monthly_invoices()`: Idempotent batch generation of `FeeInvoice` and `FeeInvoiceItem` records.
    - `record_payment()`: Concurrency-safe payment recording with sequential `REC-YYYY-XXXXX` numbering using `select_for_update()`.
    - `calculate_invoice_balance()`: Fact-derived financial ledger calculation returning `current_balance` and statuses (`Unpaid`, `Partially Paid`, `Paid`, `Overpaid`).
    - `get_defaulters_list()`: Annotated query identifying overdue invoices past `valid_until`.
  - Implemented `AttendanceWebService` in `classfellow_web/apps/attendance/services.py`:
    - `load_class_roster()`: Loads active class roster with recorded status or defaults to `Present`.
    - `save_bulk_attendance()`: Atomic upsert using `bulk_create(update_conflicts=True, unique_fields=['enrollment', 'attendance_date'])`.
    - `generate_whatsapp_payload()`: Assembles clean bilingual notifications and encoded `https://wa.me/92...` links.
  - Implemented `ExamWebService` in `classfellow_web/apps/examinations/services.py`:
    - `record_student_marks()`: Validates score boundaries ($0 \le \text{marks\_obtained} \le \text{maximum\_marks}$) and absent flag handling.
    - `calculate_class_results()`: Computes percentage aggregates, letter grades via `GradingTier`, and standard competition joint ranks (1, 2, 2, 4...).
  - Authored automated integration test suite in `tests/test_django_services.py` (9 tests passing).

*(Category 10: Phase 5 Django Web Scaffolding & ORM Mapping is 100% COMPLETE)*

---

### Category 11: Phase 5 Multi-User Web Presentation Layer & In-Memory PDF Streaming
- [x] **Task WEB-UI-01: Role-Based Web Authentication & Base Shell**
  - Implemented session authentication login and logout views in `apps/accounts/views.py` with `@role_required` access control supporting `Role.ADMIN`, `Role.PRINCIPAL`, and `Role.CASHIER`.
  - Authored `classfellow_web/templates/base.html` providing a responsive sidebar and topbar reflecting ClassFellow Slate-900 / Emerald design tokens (`#0F172A`, `#1E293B`, `#10B981`), dynamic workspace navigation tabs (Dashboard, Students, Fees, Attendance, Examinations), user session profile display, and logout trigger.
  - Authored `classfellow_web/templates/accounts/login.html` with dark glassmorphic card and institutional branding.
- [x] **Task WEB-UI-02: Responsive Workspaces & Server-Rendered Views**
  - `apps/core/views.py` (`dashboard_view`) & `classfellow_web/templates/core/dashboard.html`: Real-time KPI aggregate cards (Active Students, Today's Attendance %, Monthly Invoiced, Total Collected, Outstanding Ledger) and live cashier transaction stream.
  - `apps/students/views.py` (`student_list_view`, `student_create_view`) & `classfellow_web/templates/students/student_list.html`: Real-time multi-field search directory, class filtering, and admission form/modal delegating to `StudentWebService.register_student()`.
  - `apps/fees/views.py` (`fee_workspace_view`, `record_payment_view`, `generate_invoices_view`) & `classfellow_web/templates/fees/fee_list.html`: Tabbed invoice ledger with live fact-derived balances, overdue defaulters reporting with WhatsApp alerts, cashier payment modal generating sequential `REC-YYYY-XXXXX` receipts, and monthly batch invoicing.
  - `apps/attendance/views.py` (`attendance_roster_view`, `save_attendance_view`) & `classfellow_web/templates/attendance/roster.html`: Date and class group selector, roster grid defaulting to `Present` with status toggles (Present/Absent/Late/Leave), and one-click WhatsApp absence notification deep-links.
  - `apps/examinations/views.py` (`exam_workspace_view`, `save_marks_view`) & `classfellow_web/templates/examinations/exam_view.html`: Exam/class filters, subject tab navigation, marks entry grid with bounds validation ($0 \le \text{marks\_obtained} \le \text{maximum\_marks}$), and class rankings scorecard with competition ranks (1, 2, 2, 4...).
- [x] **Task WEB-UI-03: In-Memory A4 PDF Streaming Endpoints**
  - Added `stream_fee_voucher_pdf(request, invoice_id)` in `apps/fees/views.py` rendering print-ready 3-panel portrait A4 fee vouchers in-memory via ReportLab and `io.BytesIO` returning `HttpResponse(content_type='application/pdf')`.
  - Added `stream_report_card_pdf(request, exam_id, enrollment_id)` in `apps/examinations/views.py` assembling `StudentReportCardDTO` and streaming single-sheet bilingual A4 terminal report cards in-memory via ReportLab and `io.BytesIO`.
- [x] **Task WEB-UI-04: Web Presentation Automated Test Suite**
  - Authored `tests/test_django_web_views.py` covering authentication redirects, login credential verification, role-based permission enforcement, HTTP 200 responses across all 5 workspace GET endpoints, POST submissions (student admission, fee payment, bulk attendance, marks entry), and in-memory PDF streaming validation confirming HTTP 200, application/pdf MIME type, and `%PDF-` byte header (11 tests passing, 130 total tests passing).

*(Category 11: Phase 5 Multi-User Web Presentation Layer & In-Memory PDF Streaming is 100% COMPLETE)*

---

### Category 12: Phase 6 Multi-Campus Scoping & Financial Reconciliation
- [x] **Task MC-01: Multi-Branch & Campus Scoping Layer**
  - Implemented `Campus` model in `classfellow_web/apps/core/models.py` (`campuses` table, fields: `name`, `code`, `address`, `phone`, `email`, `is_active`, `created_at`).
  - Linked `ClassGroup` in `classfellow_web/apps/students/models.py` to `Campus` via `ForeignKey(Campus, on_delete=models.RESTRICT, null=True, blank=True, related_name='classes')`.
  - Authored automatic schema migrations `0002_campus` and `0002_classgroup_campus` using `RunPython` to seamlessly assign pre-existing classes to a default "Main Campus" (`MBC`).
- [x] **Task MC-02: Cashier Day-Closing Reconciliation Engine**
  - Implemented `CashierReconciliationService` in `classfellow_web/apps/fees/services.py`:
    - `generate_daily_closing_summary(target_date, user_id=None, campus_id=None) -> dict`: aggregates total collections by payment method (`Cash`, `BankTransfer`, `OnlineDeposit`, `Cheque`), calculates issued receipt count, starting/ending sequential receipt numbers, identifies reversed transactions with reasons and operators during closing window, and outputs structured audit ledger.
  - Implemented web view endpoint `/fees/reconciliation/` in `classfellow_web/apps/fees/views.py` rendering `classfellow_web/templates/fees/reconciliation.html` with real-time KPI tiles, payment method breakdown, transaction ledger, reversal audit log, and 3-slot print-ready signature lines (`@media print`).
- [x] **Task MC-03: Financial Audit Excel Export Engine**
  - Implemented `FeeExcelExportService` using `openpyxl` in `classfellow_web/apps/fees/reports.py`:
    - `export_monthly_collection_workbook(session_id, month_year, campus_id=None) -> io.BytesIO`: generates a styled multi-column Excel spreadsheet (`[Admission #, Student Name, Father Name, Class & Section, Tuition Fee, Total Billed, Discount, Total Paid, Balance, Status, Last Receipt #]`) applying ClassFellow Slate-900 `#1E293B` header fills, bold white text, alternating zebra striping, currency formatting (`#,##0.00`), auto-fit column widths, and summary formula totals.
  - Implemented streaming endpoint `/fees/reports/monthly-collection/xlsx/` in `classfellow_web/apps/fees/views.py` returning `HttpResponse(content_type='application/vnd.openxmlformats-officedocument.spreadsheetml.sheet')`.
- [x] **Task MC-04: Automated Verification Test Suite**
  - Authored `tests/test_multi_campus_and_reconciliation.py` containing 7 test functions validating `Campus` model relationships and class group scoping, `CashierReconciliationService` calculations (cash totals, receipt range, reversal tracking, cashier and campus filtering), openpyxl spreadsheet generation with styling/cell values and `PK\x03\x04` zip header, and HTTP 200 responses on `/fees/reconciliation/` and the XLSX streaming endpoint (137 total tests passing).

*(Category 12: Phase 6 Multi-Campus Scoping & Financial Reconciliation is 100% COMPLETE)*

---

### Category 13: Phase 6 Staff Module & Desktop-to-Cloud Sync Gateway
- [x] **Task STF-01: Teacher & Staff Administration App (`apps/staff/`)**
  - Created `apps.staff` app and registered it in `settings.py`.
  - Implemented `Staff` model (`staff_members` table) with fields: `employee_id` (unique), `first_name`, `last_name`, `urdu_name`, `designation`, `department`, `campus` (`RESTRICT`), `phone`, `email`, `national_id_cnic`, `basic_salary` (`DecimalField(12, 2)`), `joining_date`, `is_active`, and optional `user` (`SET_NULL`).
  - Implemented `StaffSubjectAllocation` model (`staff_allocations` table) linking staff, `ClassGroup`, `Subject`, and `AcademicSession` with `UniqueConstraint(fields=['staff', 'class_group', 'subject', 'academic_session'])`.
  - Authored schema migration `staff/migrations/0001_initial.py`.
- [x] **Task STF-02: Staff Management Web Workspace**
  - Implemented `staff_list_view` in `apps/staff/views.py` with multi-field search (`q`), campus filtering, status toggles (`active`, `inactive`, `all`), and real-time KPI aggregates (Total Staff, Active Count, Monthly Payroll).
  - Implemented `staff_create_view` in `apps/staff/views.py` with atomic staff registration, Pakistani mobile number normalization via `normalize_pakistan_phone`, salary Decimal validation, and duplicate employee code rejection.
  - Authored `classfellow_web/templates/staff/staff_list.html` adhering to ClassFellow Slate-900 / Emerald design tokens, with responsive KPI tiles, data table, and modal dialog.
  - Updated `classfellow_web/templates/base.html` adding `"👨‍🏫 Staff"` navigation tab.
- [x] **Task SYNC-01: Desktop-to-Cloud Backup Sync API**
  - Created `InstitutionBackupSnapshot` model in `apps/core/models.py` (`institution_backups` table) with fields: `filename`, `file_size_bytes`, `sha256_hash`, `uploaded_at`, `desktop_machine_id`, `ip_address`, and `backup_file` (`uploads/backups/`).
  - Authored schema migration `core/migrations/0003_institutionbackupsnapshot.py`.
  - Configured `INSTITUTION_SYNC_TOKEN` in `settings.py`.
  - Implemented `sync_backup_upload_view` at `POST /api/v1/sync/upload/` in `apps/core/views.py` secured with Bearer token authentication, chunked SHA-256 verification with `hmac.compare_digest()`, client metadata recording, and atomic snapshot persistence returning JSON (`201 Created`).
- [x] **Task STF-03: Automated Test Suite**
  - Authored `tests/test_staff_and_cloud_sync.py` containing 9 comprehensive automated tests verifying `Staff` model fields and Decimal precision, `StaffSubjectAllocation` unique constraint enforcement, role-protected list/create views, Pakistani phone normalization, and sync upload security (rejection of non-POST, missing/invalid Bearer token, corrupted SHA-256 checksum, and valid binary payload persistence).
  - All 146 automated tests passing across the test suite with zero regressions.

*(Category 13: Phase 6 Staff Module & Desktop-to-Cloud Sync Gateway is 100% COMPLETE)*

---

### Category 14: Phase 6 Executive Analytics & Parent Notification Gateway
- [x] **Task ANL-01: Executive Analytics Service & Metric Visualizers**
  - Implemented `ExecutiveAnalyticsService` in `classfellow_web/apps/core/analytics.py`:
    - `get_financial_recovery_metrics(session_id, month_year, campus_id=None) -> dict`: aggregates class-by-class recovery percentages `(total_collected / total_invoiced) * 100` guarded against zero-division via Decimal calculations, campus breakdowns, and detects top 5 defaulting classes ranked by unpaid balances.
    - `get_academic_risk_roster(session_id, exam_id=None, threshold_pct=Decimal("40.00")) -> list[dict]`: dynamically scans enrollments for chronic absenteeism (< 75% attendance) and academic struggle (exam marks < passing threshold across multiple subjects), compiling student risk factors, attendance percentages, and guardian contact details.
  - Implemented executive dashboard view `analytics_view` at `/analytics/` in `apps/core/views.py` and template `classfellow_web/templates/core/analytics.html` featuring real-time financial KPI cards, visual progress bars with health color coding (Emerald `>= 90%`, Amber `75-89%`, Rose `< 75%`), defaulting classes breakdown, at-risk student roster, and direct PDF audit report generation trigger.
  - Updated `classfellow_web/templates/base.html` adding `"📈 Analytics"` navigation link.
- [x] **Task NTC-01: Parent Noticeboard & WhatsApp Broadcast Feed**
  - Created `InstitutionNotice` model in `apps/core/models.py` (`institution_notices` table) with fields: `title`, `urdu_title`, `content`, `urdu_content`, `notice_type` (`NoticeType` choices: General, Academic, Fee Reminder, Holiday, Emergency), `target_campus` (optional ForeignKey with `SET_NULL`), `target_class` (optional ForeignKey with `SET_NULL`), `issued_date`, `is_published`, `created_by` (optional ForeignKey with `SET_NULL`), and `created_at`.
  - Implemented `generate_whatsapp_bulletin(institution_name=None) -> str` formatting bilingual broadcast messages with WhatsApp markdown accents, official letterhead headers, and bilingual content.
  - Authored schema migration `core/migrations/0004_institutionnotice.py`.
  - Implemented noticeboard view `notices_view` at `/notices/` in `apps/core/views.py` and template `classfellow_web/templates/core/notices.html` with notice cards, category badges, campus pills, copyable WhatsApp broadcast modal, and modal creation dialog.
  - Updated `classfellow_web/templates/base.html` adding `"📢 Notices"` navigation link.
- [x] **Task RPT-01: Monthly Executive Audit Packet PDF Generator**
  - Implemented `MonthlyAuditPacketService` in `classfellow_web/apps/core/reports.py`:
    - `generate_monthly_audit_packet(session_id, month_year, campus_id=None) -> io.BytesIO`: builds an in-memory ReportLab multi-page executive audit document adhering to ClassFellow Slate-900 `#0F172A` / Emerald `#10B981` styling:
      - **Page 1**: Executive Financial Summary & KPI overview, total invoiced/collected/outstanding, recovery rate badge, and campus-wise recovery table.
      - **Page 2**: Comprehensive Defaulters Ledger itemizing student name, admission #, class, father name, and balance.
      - **Page 3 / Final**: Formal Institutional Audit Declaration and 3-slot print-ready signature grid (Accounts Officer / Cashier, Internal Auditor / Bursar, Principal / Campus Head).
  - Implemented streaming endpoint `stream_monthly_audit_packet_pdf` at `/reports/monthly-audit-packet/<int:session_id>/<str:month_year>/pdf/` in `apps/core/views.py`.
- [x] **Task ANL-02: Automated Verification Test Suite**
  - Authored `tests/test_analytics_and_notices.py` containing 6 comprehensive automated tests validating:
    - Guarded Decimal financial recovery metric calculations and top defaulting class identification.
    - Academic risk roster detection (flagging low attendance < 75% and multiple failing subject grades).
    - `InstitutionNotice` creation, campus scoping, and WhatsApp string formatting.
    - RBAC enforcement (`ADMIN`, `PRINCIPAL`, `CASHIER`) and template rendering on `/analytics/`.
    - Notice creation and filtering on `/notices/`.
    - ReportLab in-memory monthly audit packet PDF streaming endpoint returning `%PDF-` header and 200 OK.
  - All 152 automated tests passing across the test suite with zero regressions.

*(Category 14: Phase 6 Executive Analytics & Parent Notification Gateway is 100% COMPLETE)*

---

### Category 15: Phase 7 Mobile API Gateway & Headless REST Services
- [x] **Task API-01: Django REST Framework & Token Authentication Setup**
  - Added `djangorestframework>=3.14.0,<3.16.0` to `requirements.txt` and installed in `.venv`.
  - Registered `'rest_framework'`, `'rest_framework.authtoken'`, and `'apps.api.apps.ApiConfig'` in `config/settings.py`.
  - Configured `REST_FRAMEWORK` settings with `TokenAuthentication`, `SessionAuthentication`, and default `IsAuthenticated` permission.
  - Added `TEACHER = "Teacher", "Teacher"` and `PARENT = "Parent", "Parent"` to `Role.choices` in `apps/accounts/models.py`.
  - Generated and applied schema migrations `accounts.0002_alter_user_role` and `authtoken` (0001 through 0004).
  - Implemented token issuance endpoint `POST /api/v1/auth/login/` in `apps/api/views_auth.py` validating credentials, rejecting inactive accounts with HTTP 403 Forbidden, and returning `{"token": str, "user_id": int, "username": str, "role": str, "full_name": str}`.
- [x] **Task API-02: Teacher Mobile REST API Layer**
  - In `classfellow_web/apps/api/views_teacher.py`:
    - `TeacherAssignedClassesView` (`GET /api/v1/teacher/classes/`): Returns class groups and subjects allocated to the teacher via `StaffSubjectAllocation`, with campus scoping and session metadata.
    - `TeacherRosterView` (`GET /api/v1/teacher/roster/`): Loads student attendance roster for a specific date defaulting unrecorded statuses to `Present` using `AttendanceWebService.load_class_roster()`, with teacher allocation access verification.
    - `TeacherAttendanceSaveView` (`POST /api/v1/teacher/attendance/save/`): Bulk upserts attendance records atomically via `AttendanceWebService.save_bulk_attendance()` with user audit attribution (`recorded_by_user_id`).
    - `TeacherMarksSaveView` (`POST /api/v1/teacher/marks/save/`): Submits student examination marks with bounds validation ($0 \le \text{marks} \le \text{maximum_marks}$) using `ExamWebService.record_student_marks()`, rejecting negative and overflowing marks with HTTP 400 Bad Request.
- [x] **Task API-03: Parent Portal REST API Layer**
  - In `classfellow_web/apps/api/views_parent.py`:
    - `ParentChildrenView` (`GET /api/v1/parent/children/`): Identifies children enrolled across all campuses matching the authenticated guardian's phone number, with format-agnostic Pakistani phone normalization (`03XXXXXXXXX`, `+92-300-XXXXXXX`, `0092...`).
    - `ParentFeeSummaryView` (`GET /api/v1/parent/fees/`): Returns itemized invoice breakdown, paid receipts history, total billed, total paid, and net receivables for a child's enrollment.
    - `ParentAttendanceView` (`GET /api/v1/parent/attendance/`): Returns monthly attendance calendar log, day counts (present, absent, leave, late), and calculated attendance percentages.
    - `ParentReportCardView` (`GET /api/v1/parent/report-card/`): Returns examination scorecards, subject marks breakdown, percentage, grading tier letter grade, and joint class rank via `ExamWebService.calculate_class_results()`.
    - Enforced strict IDOR (Insecure Direct Object Reference) parent-child authorization check across all child endpoints, returning HTTP 403 Forbidden on guardian-student mismatch.
- [x] **Task API-04: Automated Verification Test Suite**
  - Authored `tests/test_mobile_api_endpoints.py` containing 10 comprehensive tests covering:
    - Gating of all protected endpoints returning HTTP 401 Unauthorized without auth tokens.
    - Token generation on `/api/v1/auth/login/` and 403 rejection of inactive accounts.
    - Teacher class allocations and roster loading defaulting to Present.
    - Teacher bulk attendance upsert and audit logging.
    - Teacher marks submission bounds validation (exceeding maximum marks or negative scores).
    - Multi-campus child discovery by guardian phone with format tolerance.
    - Parent fee ledger summary and strict IDOR access rejection.
    - Parent monthly attendance aggregates and daily calendar.
    - Parent examination report card with class rank, percentage, and grade.
  - Verified full test suite pass rate: **162 of 162 automated tests passing** (152 previous + 10 new) with zero regressions.

*(Category 15: Phase 7 Mobile API Gateway & Headless REST Services is 100% COMPLETE)*

---

### Category 16: Phase 7 Cross-Platform Mobile Client (Flutter 3.x)
- [x] **Task MOB-01: Flutter Project Scaffolding & Theme System**
  - Scaffolded `classfellow_mobile/` clean architecture: `core/` (theme, network, storage), `models/` (user_session, teacher_models, parent_models), and `features/` (auth, teacher, parent).
  - Configured `pubspec.yaml` with pinned dependencies: `http: ^1.2.0`, `flutter_secure_storage: ^9.2.0`, `intl: ^0.19.0`, and `provider: ^6.1.1`.
  - Implemented `AppTheme` design system adhering strictly to ClassFellow visual tokens: Slate-900 (`#0F172A`) background, Slate-800 (`#1E293B`) cards, Emerald-500 (`#10B981`) primary accents, Slate-700 borders, Amber warnings, and Rose danger alerts.
- [x] **Task MOB-02: Authentication & Secure Session Engine**
  - Implemented `ApiClient` with 10-second request timeouts, structured `ApiException` hierarchy, and `Authorization: Token <key>` interceptor.
  - Implemented `SecureStorageService` providing encrypted session caching with in-memory fallback for headless test environments.
  - Implemented `AuthController` managing authentication state transitions, persistent credentials hydration, and role identification.
  - Built `LoginScreen` featuring institutional branding, form validation, error banners, and dynamic role-based routing (Teacher -> `TeacherHomeScreen`, Parent -> `ParentHomeScreen`).
- [x] **Task MOB-03: Teacher Mobile Experience**
  - Built `TeacherHomeScreen` displaying assigned class allocations, subjects, campus facility, and academic session, with quick actions and secure sign-out.
  - Built `AttendanceRosterScreen` featuring interactive date picker, full class roster, quick "All Present" batch action, segmented P/A/L/T status toggles, and atomic bulk save to `/api/v1/teacher/attendance/save/`.
  - Built `MarksEntryScreen` with real-time numeric bounds validation ($0 \le \text{marks} \le \text{max\_marks}$), absent checkboxes, out-of-bounds error feedback, and batch marks submission.
- [x] **Task MOB-04: Parent Portal Mobile Experience**
  - Built `ParentHomeScreen` with horizontal scrolling multi-child switcher chips and bottom navigation bar across three dedicated tabs.
  - Built `ParentFeeTab` displaying 3-metric financial summary (Billed, Paid, Balance), itemized invoice history with status chips, and payment receipt records.
  - Built `ParentAttendanceTab` featuring monthly navigation controls, circular attendance percentage gauge, status counts (Present, Absent, Leave, Late), and day-by-day logs.
  - Built `ParentReportCardTab` displaying examination overview, class rank badge, overall percentage, letter grade, and subject-by-subject scorecards.
- [x] **Task MOB-05: Verification & Contract Test Suite**
  - Authored Flutter Dart unit test suite in `classfellow_mobile/test/` (`models_test.dart`, `api_client_test.dart`, `auth_routing_test.dart`) covering JSON serialization, network timeout error mapping, token injection, and session role routing.
  - Authored backend contract verification suite in `tests/test_mobile_contract.py` testing all 9 mobile client contract requirements against Django REST Framework endpoints.
  - Verified full test suite pass rate: **171 of 171 automated tests passing** (162 previous + 9 contract tests) with zero regressions.

*(Category 16: Phase 7 Cross-Platform Mobile Client is 100% COMPLETE)*

---

### Category 17: Phase 8 Production Hardening, Dockerization & Staging Deployment
- [x] **Task SEC-01: Production Hardening & Security Middleware**
  - Added `gunicorn>=21.2.0` and `whitenoise>=6.6.0` to `requirements.txt`.
  - Implemented `classfellow_web/config/settings_prod.py` enforcing `DEBUG = False`, dynamic `ALLOWED_HOSTS`, HTTPS/HSTS redirects, secure cookie flags (`SESSION_COOKIE_SECURE`, `CSRF_COOKIE_SECURE`, `SECURE_SSL_REDIRECT`), WhiteNoise compressed manifest static files storage (`WHITENOISE_MANIFEST_STRICT = False`), and structured console logging.
  - Implemented unauthenticated, rate-limit-exempt `/health/` probe in `apps/core/views.py` (`health_check_view`) verifying database connectivity via `connection.ensure_connection()` and returning HTTP 200 on success and HTTP 503 on database connection failure. Registered `/health/` in `classfellow_web/config/urls.py`.
- [x] **Task DOCK-01: Multi-Stage Dockerization & Reverse Proxy Infrastructure**
  - Authored multi-stage `Dockerfile` (builder + runner stages) based on `python:3.12-slim` running as non-root user `classfellow:classfellow` (UID 1001), with explicit ownership of `/app/classfellow_web/staticfiles`, `/app/classfellow_web/media`, and `/app/data/backups`.
  - Authored `docker/entrypoint.sh` with Unix LF line endings (`\n`) and executable permissions, performing automated database readiness checks, pending schema migrations, and static asset collection before spawning Gunicorn workers.
  - Authored `nginx/nginx.conf` reverse proxy configuration featuring a 50MB client max body size, direct caching for `/static/` and `/media/`, bypass routing for `/health/`, and standard proxy headers (`X-Forwarded-For`, `X-Forwarded-Proto`, `Host`).
  - Authored `docker-compose.yml` orchestrating `web` (Django Gunicorn), `db` (PostgreSQL 16 Alpine), and `nginx` (Nginx 1.25 Alpine) with container health checks, network isolation, and persistent named volumes (`postgres_data`, `static_volume`, `media_volume`, `backup_volume`).
- [x] **Task OPS-01: Staging Provisioning Utility & Database Seeding**
  - Implemented `scripts/provision_instance.py` enabling idempotent institutional provisioning with zero-drift financial integrity.
  - Seeds admin account (`admin` / default password), primary institutional campus (`Main Campus - Lahore`), active academic session (`2026-2027`), baseline fee heads (`Tuition Fee`, `Annual Exam Fee`, `Admission Fee`, `Laboratory Charges`) using strict `Decimal` monetary amounts, and standard Punjab BISE grading tiers (A+, A, B, C, D, E, F).
- [x] **Task TEST-01: Production Smoke Testing & Regression Verification**
  - Authored comprehensive production test suite `tests/test_production_smoke.py` containing 6 test cases verifying production settings security flags, health check 200/503 behavior, static files collection readiness, provisioning idempotency, and container configuration syntax (Dockerfile, Compose, Nginx, Entrypoint).
  - Verified full test suite pass rate: **177 of 177 automated tests passing** (171 previous + 6 new) with zero regressions across the entire platform.

*(Category 17: Phase 8 Production Hardening, Dockerization & Staging Deployment is 100% COMPLETE)*

---

### Phase 9: Technical Auditing, UI/UX Branding, Field Maintenance, and Marketing Launch
- [x] **Task P9-ROADMAP: Master Roadmap Formulation & Operational Blueprint**
  - **File**: `docs/roadmaps/PHASE_09_VALIDATION_REFINEMENT_AND_MAINTENANCE_ROADMAP.md`
  - **Scope**: Authored 6 core tracks:
    1. **Track 1**: Step-by-Step Desktop Audit & USB Deployment Plan (`ClassFellow.exe` PyInstaller/Inno Setup USB distribution and 7-part Windows 11 tester audit checklist).
    2. **Track 2**: Refinement & Self-Criticism Framework (PO observation log, sibling discounts, pro-rated mid-month admissions, arrears auto-roll forward, session promotion wizard, cashier day-closing reconciliation).
    3. **Track 3**: NotebookLM EdTech Competitive Benchmarking Guide & Prompts (PakSchool, SkoolSys, M-Skins comparative gap analysis).
    4. **Track 4**: UI/UX & Visual Identity Enhancement Plan (Logo positioning, opacity-controlled ReportLab watermark engine, 4 institutional color palettes, low-light ergonomics).
    5. **Track 5**: On-Site Field Support & Maintenance-Kit Architecture (`tools/maintenance/` recovery utilities and `manifest.json` in-place updater).
    6. **Track 6**: Institutional Web Portal & Launch Strategy (`classfellow.com` bilingual landing page, sample PDF showcase, changelog hub).
- [x] **Task P9-STEP1: Standalone Portable Desktop Build & USB Assembly**
  - **Outputs**: `dist/ClassFellow/ClassFellow.exe`, `dist/ClassFellow_Portable_USB/`
  - **Features**: Pre-flight font path guard (`sys._MEIPASS`) in `fee_voucher_generator.py` and `report_card_generator.py`, TrueType Urdu font (`assets/fonts/urdu.ttf`) bundled in `_internal`, standalone process verified running without crash (PID 10144), sample Excel roster (`students_import_sample.xlsx`) generated, SHA-256 hash verified (`28A369DBB84B843BCB90FF6A851C68210138B6F9EC139343F38864233A06A2D5`), and full regression test suite passing 177/177.
  - **Status**: Complete and ready for Product Owner physical testing on Windows 11.
- [x] **Task P9-TRACK1-AUDIT: Windows 11 Physical USB Testing & Defect Resolution (BUG-001, BUG-002, BUG-003)**
  - **File**: `docs/troubleshooting/BUGS_AND_RESOLUTION_REGISTER.md`
  - **Scope**: Catalog and resolve all defects discovered during physical testing on Windows 11:
    - `BUG-001 (CRITICAL)`: Cold-start database auto-bootstrap via `init_database()` in `app/app.py`, error recovery card on view swap, and empty-table guards in `ui/student_view.py`, `ui/attendance_view.py`, and `ui/exam_view.py`.
    - `BUG-002 (MEDIUM)`: Pixel-perfect sidebar navigation button alignment using two-column compound `SidebarNavButton` (36px centered icon box + uniform text margin).
    - `BUG-003 (HIGH)`: Active System Diagnostics & Database Health card in `ui/settings_view.py` with live engine metrics, table row-count probes, and 1-click `🛠️ Verify & Auto-Repair Database` utility.
  - **Verification**: `tests/test_cold_boot.py` added; full regression suite 179/179 passing (100% green); recompiled `ClassFellow.exe` (SHA256: `B61BE4BBA7CD34BC586E29E1CB44D7CF1672A7DAD4BD23FB5EA68CBA2E8938E9`) updated in `dist/ClassFellow_Portable_USB/`.
  - **Status**: Complete — Ready for physical re-verification on Windows 11.
- [ ] **Task P9-AUDIT2: Physical Inspection #2 Findings & Architectural Analysis (BUG-004, BUG-005, BUG-002B, REF-001) [IN PROGRESS]**
  - **File**: `docs/troubleshooting/BUGS_AND_RESOLUTION_REGISTER.md`
  - **Scope**:
    - `BUG-004 (HIGH)`: Urdu Name field enforces LTR typing instead of native RTL BiDi flow in CustomTkinter.
    - `BUG-005 (CRITICAL)`: "Assign Class" dropdown displays unmapped "Default Class" on fresh database, triggering blocking "Validation Error: Please select a valid class group." that prevents new admissions.
    - `BUG-002B (MEDIUM)`: Windows DirectWrite ZWJ emoji dissociation (`👨‍🎓` $\rightarrow$ `👨` + `🎓`) causing double icon glyphs and sidebar misalignment.
    - `REF-001 (ARCHITECTURAL)`: Admission Modal fee charges section redesign & discount semantics (Held pending competitor form samples from Chief Architect).
  - **Status**: Root causes analyzed, comparative trade-offs documented, and submitted for Chief Architect and GEM AI review. Zero application code modified until sign-off.

*(Phase 9 Physical Inspection #2 Defect Analysis Complete — Pending Architectural Direction)*

---


## 2. Approved Technical Caveats & Engineering Rules

1. **DPI Initialization Guard**: CustomTkinter handles DPI internally on startup. Any explicit call to `SetProcessDpiAwareness(2)` must be wrapped in a `try...except Exception: pass` block to prevent `E_ACCESSDENIED` crashes on Windows 11.
2. **Offline Schema Migration Mechanism**: Because school computers operate without internet-driven DB admin tools, `database.py` must track migration states via `PRAGMA user_version` (or a `schema_migrations` table) and auto-apply missing DDL scripts at startup.
3. **Native SQLite Online Backup API**: Never copy active SQLite WAL files via raw `shutil.copy2`. All daily and USB backup routines must invoke `src_conn.backup(dest_conn)`.
4. **Strict Financial Ledger Foreign Key Rule**: Invoices cannot be orphaned or deleted if a payment receipt exists. All payment tables enforce `FOREIGN KEY (invoice_id) REFERENCES fee_invoices(id) ON DELETE RESTRICT`.

---

## 3. Approved Execution Sequence

1. **Step 1: Formal SRS Document Generation**  
   - Produce `docs/srs/SRS_00_Overview_and_Modular_Architecture.md`  
   - Produce `docs/srs/SRS_01_Core_Kernel_and_Database_Spec.md`  
   - Produce `docs/srs/SRS_02_Student_and_Enrollment_Spec.md`  
   - Produce `docs/srs/SRS_03_Fee_and_Receipt_Module_Spec.md`  
   - Produce `docs/srs/SRS_04_Attendance_and_Notification_Spec.md`  
   - Produce `docs/srs/SRS_05_Examination_and_ReportCard_Spec.md`
2. **Step 2: Git Repository & Version Control Setup** (Tasks GIT-01 to GIT-04)
3. **Step 3: Codebase Directory Scaffolding & Dependencies** (Tasks CODE-01 to CODE-02)
4. **Step 4: Core Engine Implementation & Automated Verification** (Tasks CODE-03 to CODE-06, BKP-01, MOD-01)
